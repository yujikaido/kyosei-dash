node_modules
.DS_Store
dist
dist-ssr
*.local
.idea

/data
!/data/.gitkeep
/data*
.vscode

/private
/out
/tmp
.env

/extra/healthcheck.exe
/extra/healthcheck
/extra/healthcheck-armv7

extra/exe-builder/bin
extra/exe-builder/obj

.vs
.vscode

# Local sync output — not part of repo
/updates/

# Local debug scratch — not part of repo
/debug/

# Kuma's seed DB — Kyosei runs DB inside the container's mounted volume
db/*.db
db/*.db-journal

# Backup imports — local only, may contain sensitive cleartext credentials
/UPKuma backup json/
*.uptime-kuma-backup.json
